# Two-sample adapter startup check. This is not a quality or benchmark run.
name4train: "rolloutremover-cogvideox5b-smoke-480x720-v2"
log_level: "INFO"

model_name: "cogvideox-rollout-t2v"
model_path: "/mnt/cpfs/jiachengliu/pretrained_models/CogVideoX-5b"
output_dir: "/mnt/cpfs/jiachengliu/dataset/CORNE/ckpt/cogkit_rolloutremover_cogvideox5b_smoke_480x720_v2"
report_to: null

data_root: "/mnt/cpfs/jiachengliu/dataset/CORNE"
rollout_manifest: "/mnt/cpfs/jiachengliu/dataset/CORNE/cache/cogvideox_rollout_5b_14f_mode_480x720_smoke_20260806/manifest_2.jsonl"
rollout_cache_dir: "/mnt/cpfs/jiachengliu/dataset/CORNE/cache/cogvideox_rollout_5b_14f_mode_480x720_smoke_20260806"
rollout_prompt: "Remove the masked object and its side effect"
rollout_preset: "wan14b_maskpred_c4r5_g1p2_14f"
rollout_expected_vae_scaling_factor: 0.7
rollout_expected_posterior_mode: "mode"

seed: 42
training_type: "lora"
strategy: "DDP"
low_vram: false
offload_params_grads: false
no_grad_sync_when_accumulating: false
enable_packing: false
enable_slicing: true
enable_tiling: true
train_resolution: [14, 480, 720]
train_epochs: 1
batch_size: 1
gradient_accumulation_steps: 1
mixed_precision: "bf16"
learning_rate: 1.0e-4
optimizer: "adamw"
beta1: 0.9
beta2: 0.95
epsilon: 1.0e-8
weight_decay: 1.0e-4
max_grad_norm: 1.0
lr_scheduler: "CosineAnnealingLR"
gradient_checkpointing: true
num_workers: 0
pin_memory: false
checkpointing_steps: 1
checkpointing_limit: 3
resume_from_checkpoint: null

do_validation: false
validation_steps: null
gen_fps: 8
